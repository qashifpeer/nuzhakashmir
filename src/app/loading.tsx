import { Loader } from "@/components";
import { motion, useInView } from "framer-motion";

const LoadingPage = () => {
  return (
    <div>
      <Loader />
    </div>
  )
}

export default LoadingPage